# AGENTS.md

このファイルは、コーディングエージェントがプロジェクトの目的・技術スタック・コーディング規約を理解するための指示書です。

## 🤖 エージェントへの指示（重要）

- **コミュニケーション言語:** 提案・質問・結果の要約など、すべての対話は**必ず日本語**で行ってください。

## 🚀 プロジェクトの目的

自動的にセルが拡張する事で無限に広がる盤面でプレイできる**マインスイーパー**をベースとしたゲーム。詳細な要件は [docs/overview.md](docs/overview.md) を参照。

## 🛠️ 技術スタック

- **フロントエンド:** React 19（TypeScript）
- **ビルドツール:** Vite
- **スタイリング:** Tailwind CSS 4 + Shadcn/UI
- **状態管理:** Context API（Redux・Zustandは現時点では使用しない）
- **永続化:** Dexie.js（IndexedDB）

詳細は [README.md](README.md) を参照。

## 📏 コーディング規約

コーディング規約・プロジェクト構造・利用可能なスクリプトは [docs/develop.md](docs/develop.md) にまとめている。エージェントは作業前に必ずこちらを参照すること。

特に以下は厳格に守ること:

- 型エラーは厳格に修正する
- 未使用の変数は削除する
- `any` の使用は避ける

## 📐 設計・計画ドキュメント

重要機能や大規模改修を行う際は、着手前に [docs/designs/](docs/designs/) 配下の関連ドキュメントを確認すること。既存の設計方針（例: チャンク単位のデータ管理、座標変換ロジック）と矛盾する実装をしないよう注意する。

## ✅ 作業完了前のチェック

- `pnpm lint` でエラーがないことを確認する
- `pnpm test` でテストが通ることを確認する
- 必要に応じて `pnpm build` で型エラーがないことを確認する

コマンドの詳細は [docs/develop.md](docs/develop.md) を参照。
